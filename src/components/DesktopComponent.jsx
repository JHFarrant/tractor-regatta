import Typography from "@mui/material/Typography";
import React from "react";

const DesktopComponent = () => {

    return (
        <Typography variant="h3">
            This website can only be accessed on a mobile device
        </Typography>
    );
}

export default DesktopComponent
